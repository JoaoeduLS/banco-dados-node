// import type { HttpContextContract } from '@ioc:Adonis/Core/HttpContext'

export default class CursosController {
    store(){
        const dados= {nome:'Joao',duracao:2, cursoId:'p'}
        return dados
    }
    
}
